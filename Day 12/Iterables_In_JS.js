// in Array

console.log("Arrays");
let array = [1, 2, 3, 4, 5];

for (let element of array) {
    console.log(element);
}

// Output: 1, 2, 3, 4, 5


// Strings

console.log("Strings");
let str = "Hello";

for (let char of str) {
    console.log(char);
}

// Output: H, e, l, l, o
